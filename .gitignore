# Local demo secrets — never commit (see the Quickstart in README.md)
config/secrets.yml

# Customer verification overlay (real hostnames/paths) — never commit
config/verify.customer.yml

# Ansible runtime artifacts
*.retry
.tmp/
.ansible/
/collections/

# Verification run output
reports/

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/
